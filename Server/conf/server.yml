# PONAPI server & repository configuration file

server:
  sort_allowed: "false"
  send_version_header: "true"
  links_type: "relative"

repository:
  class:  "Test::PONAPI::DAO::Repository::MockDB"
  args: []
